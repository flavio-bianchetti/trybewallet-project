import DefaultValues from './DefaultValues';

const {
  defaultBGColor,
  defaultTextColor,
} = DefaultValues;

const DataTable = {
  classNameHeader: `${defaultBGColor} ${defaultTextColor} table-auto w-full mt-2 p-1 rounded-md shadow-md`,
//   textColorHeader: `${defaultTextColor} p-1`,
};

export default DataTable;
