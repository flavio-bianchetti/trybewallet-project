import DefaultValues from './DefaultValues';

const {
  defaultBGColor,
  defaultTextBGColor,
} = DefaultValues;

const DataFieldset = [
  {
    className: `${defaultBGColor} ${defaultTextBGColor}
    mt-2 p-1 rounded-md w-auto shadow-md`,
  },
];

export default DataFieldset;
